# WSL2：skew 与 icount 的 host time 性能报告

测试日期：2026-09-14。测试二进制基于提交 `6c4d0bad55`，新增代码仅为基准 guest、计时插件和运行脚本。

## 实测结论

固定总工作量、6 vCPU 时，icount 中位耗时 **6.963 秒**，skew 为 **3.449 秒**：**2.02 倍加速，耗时降低 50.5%**。
同一任务在单核时，加速比为 **0.86 倍**，耗时降低 **-15.6%**；负数表示 skew 更慢。
4 vCPU 的加速比为 **2.10 倍**；本次 6 vCPU 的 skew 中位耗时未优于 4 vCPU，表现出扩展收益停滞。6 vCPU 下普通 MTTCG 为 **1.697 秒**，skew 比它多耗时 **103.3%**，时间协调仍有明显成本。未通过 profiling 归因具体瓶颈。

这些结论仅适用于本机、固定配置及本报告的裸机负载。barrier 的高加速比包含单线程轮转下的自旋等待成本，不能解释为算术执行本身提高了同样倍数。

## 环境与方法

- WSL2 Ubuntu 20.04；Intel Core i7-10750H，6 个物理核、12 个逻辑 CPU；guest 为 AArch64 cortex-a57、virt/GICv2、128 MiB RAM。
- QEMU 使用 -O2，保留调试信息与断言；同一个 QEMU 二进制、同一个配置对应的 guest ELF；icount 使用 thread=single、shift=0、sleep=off。
- skew 使用 thread=multi、window=1 ms、IPS=10^9、update=100 μs。普通 MTTCG 为辅助基线。
- 本报告的普通 QEMU 基线是同一二进制使用 `-accel tcg,thread=multi`，关闭 skew 和 icount；它不是另行编译的未修改上游 QEMU。计时插件及 guest ELF 与其他模式相同。
- 主指标：guest 所有核就绪后 START MMIO 写，到所有核独立完成并校验后，由最后完成核发出的 DONE MMIO 写之间的 CLOCK_MONOTONIC 墙钟时间。两次写由同一插件在宿主侧记时。
- 排除进程启动和 CPU 启动；包含同步、自旋、host 抢占、窗口等待、测量区内首次翻译及结果校验。无 trace、无宿主延迟注入。
- 42 个配置，各一次预热、7 次正式运行：42 次预热 + 294 次正式样本。每次是新 QEMU 进程，预热不保留 TB 缓存。随机交错顺序，固定种子 20260914。
- 未设置宿主线程绑核、固定频率或独占 Windows/WSL；不删除波动样本。
- 原计划最快样本约 5 秒，为控制完整矩阵耗时，实际调整为约 1 秒以上，使用 7 次重复及范围呈现波动。

## 主测试：固定总工作量

总计 720,384,000 次循环，循环体每次 32 条 guest 指令，合计 23,052,288,000 条有用指令；随核数平均分配。控制及自旋指令另计，但其耗时包含在成绩内。

| vCPU | 普通 MTTCG 秒 | icount 秒 | skew 秒 | 相对 icount 加速比 | 相对 icount 耗时降低 | 相对 MTTCG 耗时增加 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 6.838 | 7.022 | 8.120 | 0.86× | -15.6% | 18.7% |
| 2 | 3.893 | 6.963 | 4.935 | 1.41× | 29.1% | 26.8% |
| 4 | 2.255 | 6.999 | 3.335 | 2.10× | 52.4% | 47.9% |
| 6 | 1.697 | 6.963 | 3.449 | 2.02× | 50.5% | 103.3% |

多核场景下 skew 快于 icount，但仍慢于普通 MTTCG。以 6 核为例，skew 耗时为普通 MTTCG 的 **2.03 倍**；这是该配置下启用时间协调后的整体成本，不代表已定位到某个具体函数的开销。

相对 MTTCG 耗时增加 = (median(T_skew) / median(T_MTTCG) - 1) × 100%。此列与相对 icount 的耗时降低使用不同基准。

## 全部场景与波动范围

时间列为 **中位数 [最小, 最大]**，单位秒。每行不同模式的有用工作量相同。

| 场景 | vCPU | icount | skew | 普通 MTTCG | 相对 icount 加速比 | 相对 icount 耗时降低 |
|---|---:|---:|---:|---:|---:|---:|
| 固定总量 | 1 | 7.022 [6.915, 7.935] | 8.120 [7.779, 8.861] | 6.838 [6.718, 7.457] | 0.86× | -15.6% |
| 固定总量 | 2 | 6.963 [6.830, 8.639] | 4.935 [4.673, 5.584] | 3.893 [3.793, 5.105] | 1.41× | 29.1% |
| 固定总量 | 4 | 6.999 [6.750, 7.658] | 3.335 [3.147, 4.623] | 2.255 [2.231, 2.392] | 2.10× | 52.4% |
| 固定总量 | 6 | 6.963 [6.922, 7.088] | 3.449 [3.223, 3.965] | 1.697 [1.604, 1.787] | 2.02× | 50.5% |
| 固定每核量 | 1 | 1.303 [1.161, 1.397] | 1.471 [1.235, 1.642] | 1.316 [1.106, 1.588] | 0.89× | -12.9% |
| 固定每核量 | 2 | 2.412 [2.385, 2.584] | 1.659 [1.621, 1.960] | 1.312 [1.257, 1.419] | 1.45× | 31.2% |
| 固定每核量 | 4 | 4.798 [4.655, 5.204] | 2.243 [2.074, 3.071] | 1.510 [1.455, 1.594] | 2.14× | 53.2% |
| 固定每核量 | 6 | 6.972 [6.902, 7.625] | 3.356 [3.264, 3.551] | 1.797 [1.654, 1.855] | 2.08× | 51.9% |
| 128 次 barrier | 2 | 48.602 [48.224, 50.432] | 4.698 [4.598, 4.788] | 3.914 [3.771, 4.440] | 10.35× | 90.3% |
| 128 次 barrier | 4 | 85.344 [84.719, 90.166] | 3.270 [2.661, 3.462] | 2.359 [2.170, 2.579] | 26.10× | 96.2% |
| 128 次 barrier | 6 | 84.215 [83.172, 85.843] | 3.414 [3.100, 3.600] | 1.780 [1.714, 2.029] | 24.67× | 95.9% |
| 工作量不均 | 2 | 7.129 [6.941, 7.399] | 6.157 [5.850, 6.338] | 4.992 [4.818, 5.419] | 1.16× | 13.6% |
| 工作量不均 | 4 | 6.978 [6.778, 7.611] | 4.223 [4.158, 4.615] | 3.422 [3.190, 3.665] | 1.65× | 39.5% |
| 工作量不均 | 6 | 7.134 [6.916, 7.467] | 4.242 [4.009, 4.778] | 2.540 [2.497, 2.745] | 1.68× | 40.5% |

## 如何理解结果

- **固定每核量**：每核 120,064,000 次循环，核数越多总工作量越大；同一核数下可以比较模式间速度，不能直接把不同核数的 host 秒数当作同一任务的强扩展性。
- **barrier**：固定总量分成 128 段，每段全核同步。无周期性 guest 定时器时，icount_get_limit 可取 INT32_MAX，再按核数分配预算；自旋可能长时间等待其他 vCPU 获得执行机会。此项衡量同步与调度组合效果。
- **工作量不均**：最后一核取得其他核两倍工作量，各核完成后直接 WFI，不保留 CPU0 收尾自旋。该项观察负载不均及活跃成员退出的影响，仍不能替代真实 OS 的阻塞唤醒负载。
- **MTTCG 基线**：反映不加时间窗口约束时的耗时。skew 即使快于 icount，也可能明显慢于普通 MTTCG。
- 单核 1 ns/指令的名义换算相同，不代表多核时间模型相同。全部任务由固定次数控制，没有按 guest 时间运行固定时长。
- 算术循环的数据依赖较强、访存很少，不代表 Linux、缓存/内存密集型应用或任意 window 的结果。未在本轮扫描 window 参数。

## 验证与复现

早期试跑发现 CPU0 收尾轮询会污染固定计算成绩，正式版本改为每核完成即 WFI、最后一核发出 DONE。本报告只使用修正后的 v2 数据，不混入早期样本。

所有正式样本均正常退出，且通过模 2^64 Fibonacci 结果校验；倍增校验算法与循环体实现独立。脚本检查每次恰好产生一个 host 时间结果。报告生成时核对 42 组各有 7 个不同轮次，并检查不同模式的工作量一致。

加速比 = median(T_icount) / median(T_skew)。耗时降低 = (1 - median(T_skew)/median(T_icount)) × 100%。

详细实现与命令见 [基准说明](../../SKEW_ICOUNT_HOST_BENCH_zh.md)。原始数据含逐次完整 QEMU 命令：

- [逐次数据 runs.jsonl](runs.jsonl)
- [汇总 summary.json](summary.json)
- [宿主环境与二进制 SHA-256](environment.txt)

```sh
export PKG_CONFIG_PATH=/home/andy/qemu-build-deps/glib/lib/pkgconfig
python3.9 tests/tcg/aarch64/system/skew-perf.py --output build/skew-perf-new --work 720384000 --rounds 7
python3.9 tests/tcg/aarch64/system/skew-perf-report.py build/skew-perf-new docs/clock-model/benchmarks/new
```
